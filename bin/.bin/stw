#!/usr/bin/env bash
P=~/.st_font
D=Inconsolata:pixelsize=36:antialias=true:autohint=false
test -f $P || echo "$D" > $P
mkdir -p /tmp/st-out
num=`shuf -i 1-1000000000 -n 1`
#-o /tmp/st-out/$num 
st -f "`cat ~/.st_font | head -n1`" "$@"
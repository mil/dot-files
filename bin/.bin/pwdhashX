#!/usr/bin/env bash
DOMAIN=$(echo 'Domain:' | dmenu -b | tr -d '\n')
PASS=$(echo 'Password:' | dmenu -b -nf "#e8e8e8" -nb "#e8e8e8" | tr -d '\n')
ENCP=$(/home/mil/.bin/pwdhash $DOMAIN $PASS | tr -d '\n')
xdotool type --clearmodifiers "$ENCP"

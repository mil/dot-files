#!/usr/bin/env bash
DOMAIN=$(cat /home/mil/.domains | rofi -dmenu  | tr -d '\n')
PASS=$(echo 'Enter pass' | dmenu -b -nf "#ffffff" -nb "#ffffff" | tr -d '\n')
ENCP=$(/home/mil/.bin/Utilities/pwdhash $DOMAIN $PASS | tr -d '\n')
xdotool type --clearmodifiers "$ENCP"
